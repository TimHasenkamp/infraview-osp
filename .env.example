# Required — generate with: openssl rand -hex 32
JWT_SECRET_KEY=change-me-jwt
AGENT_API_KEY=change-me-agent

# Optional — defaults to SQLite in data/
# DATABASE_URL=sqlite+aiosqlite:///data/infraview.db
